c
Order.where("created_at >= ? and created_at <= ?",order_details_update["status"]["from_date"],order_details_update["status"]["to_date"]).update_all(status: "completed")
order_details_update["status"]["to_date"]
order_details_update["status"]
order_details_update["status"][:to_date]
Order.where("created_at >= ? and created_at <= ?",order_details_update["status"][:from_date],order_details_update["status"][:to_date]).update_all(status: "completed")
c
Order.where("created_at >= ? and created_at <= ?",starting,ending).update_all(status: "completed").class
Order.where("created_at >= ? and created_at <= ?",starting,ending).update_all(status: "completed")
